# Flutter/Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.g.dart.bak

# Native platform folders are generated locally via `flutter create .`
# (see docs/SETUP.md) and intentionally not committed.
# /web/ IS committed — its index.html/manifest.json are hand-customized
# (see web/index.html, web/manifest.json) and Vercel's build needs it
# present in the repo to run `flutter build web` against.
/android/
/ios/
/macos/
/linux/
/windows/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local
